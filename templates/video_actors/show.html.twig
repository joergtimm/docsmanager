{% extends 'base.html.twig' %}

{% block title %}VideoActors{% endblock %}

{% block body %}
    <h1>VideoActors</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ video_actor.id }}</td>
            </tr>
            <tr>
                <th>CreateAt</th>
                <td>{{ video_actor.createAt ? video_actor.createAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_video_actors_index') }}">back to list</a>

    <a href="{{ path('app_video_actors_edit', {'id': video_actor.id}) }}">edit</a>

    {{ include('video_actors/_delete_form.html.twig') }}
{% endblock %}
