{% extends 'base.html.twig' %}

{% block title %}Mandnat{% endblock %}

{% block body %}
    <h1>Mandnat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mandnat.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ mandnat.name }}</td>
            </tr>
            <tr>
                <th>ConId</th>
                <td>{{ mandnat.conId }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_mandnat_index') }}">back to list</a>

    <a href="{{ path('app_mandnat_edit', {'id': mandnat.id}) }}">edit</a>

    {{ include('mandnat/_delete_form.html.twig') }}
{% endblock %}
