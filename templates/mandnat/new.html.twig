{% extends 'modalBase.html.twig' %}

{% block title %}New Client{% endblock %}

{% block main_content %}
    <div class="m-4 p-4 modal:m-0 modal:p-0 bg-gray-50 rounded-lg">
        <div class="flex justify-between">
            <h1 class="text-xl font-semibold text-gray-800 mb-4">Create New Client</h1>

            <div class="mr-4">
                <a href="{{ path('app_mandnat_index') }}" class="text-sm font-medium text-gray-800">
                    Back to list
                </a>
                <div class="inline-block">
                    {% if form.vars.data.id|default %}
                        {{ include('mandnat/_delete_form.html.twig') }}
                    {% endif %}
                </div>
            </div>
        </div>
        {{ include('mandnat/_form.html.twig') }}
    </div>
{% endblock %}

{% block stream_success %}
    <turbo-stream action="prepend" targets="#mandant-list tbody">
        <template>
            {{ include('mandnat/_row.html.twig') }}
        </template>
    </turbo-stream>
{% endblock %}