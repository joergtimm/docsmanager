<form method="post" action="{{ path('app_mandnat_delete', {'id': mandnat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mandnat.id) }}">
    <button class="btn">Delete</button>
</form>
